services:
  prod:
    build:
      context: .
      dockerfile: Dockerfile
    image: threeoneaviation:latest
    ports:
      - "8000:8000"
    env_file:
      - .env.prod
    container_name: threeoneaviation_prod

  dev:
    build:
      context: .
      dockerfile: Dockerfile
    image: threeoneaviation:latest
    ports:
      - "8000:8000"
    env_file:
      - .env.dev
    container_name: threeoneaviation_dev